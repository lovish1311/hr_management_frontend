import 'package:flutter/material.dart';

// Package: hr_management

class AppColors {
  // Brand & Primary Colors
  static const Color primaryAccent = Color(0xFF0D9488); // Emerald Teal
  static const Color primaryDark = Color(0xFF0F766E);
  static const Color secondaryAccent = Color(0xFF0284C7); // Sky Blue

  // Backgrounds
  static const Color background = Color(0xFFF8FAFC); // Clean Light Slate
  static const Color darkBackground = Color(0xFF0F172A); // Deep Navy Slate
  static const Color cardWhite = Color(0xFFFFFFFF);
  static const Color cardDark = Color(0xFF1E293B);

  // Borders
  static const Color borderLight = Color(0xFFE2E8F0);
  static const Color borderDark = Color(0xFF334155);

  // Accents & Gradients
  static const Color accentBlue = Color(0xFFE0F7FA);
  static const Color accentGreen = Color(0xFFE8F5E9);

  // Stat Gradients
  static const List<Color> totalEmployeesGradient = [Color(0xFF6366F1), Color(0xFF4F46E5)]; // Indigo
  static const List<Color> presentTodayGradient = [Color(0xFF10B981), Color(0xFF059669)]; // Emerald
  static const List<Color> onLeaveGradient = [Color(0xFFF59E0B), Color(0xFFD97706)]; // Amber

  // Text Colors
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color darkTextPrimary = Color(0xFFF8FAFC);
  static const Color darkTextSecondary = Color(0xFF94A3B8);
}
